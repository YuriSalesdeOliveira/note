(function(){function showModal(modalContainer){var modal=document.querySelector(`.${modalContainer}`);modal.classList.add('modal_show');modal.addEventListener('click',(e)=>{classes=e.target.className.split(' ');if(classes[0]==modalContainer){modal.classList.remove('modal_show')}})}
var button=document.querySelector('.add_note_button');button.addEventListener('click',()=>showModal('modal_container'))})();(function(){function toggle(elementClass){element=document.querySelector(`.${elementClass}`);if(element.classList.contains('show')){element.classList.remove('show');return}
element.classList.add('show')}
var button=document.querySelector('.profile_dropdown_button');button.addEventListener('click',()=>toggle('profile_dropdown'))})();(function(){let notes=document.querySelectorAll('.notes_item');function showModalForEdit(element){function showModalWithContent(modalContainer,content,title){var modal=document.querySelector(`.${modalContainer}`);modal.classList.add('modal_show');console.log(modal.children[0].children[0].children[0].children)
modal.addEventListener('click',(element)=>{if(element.target.classList.contains(modalContainer))
modal.classList.remove('modal_show')})}
let childrens=element.children;for(let children of childrens){let content=children.className==='notes_item_content'?children.innerText:null;let title=children.className==='notes_item_title'?children.innerText:null;showModalWithContent('modal_container',content,title)}}
notes.forEach((element)=>{element.addEventListener('click',()=>showModalForEdit(element))})})()